on: [ 'push' ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - name: Copy all updated files to scripts folder
      working-directory: ${{ github.workspace }}
      run: cp */ /home/cloud/scripts --overwrite
